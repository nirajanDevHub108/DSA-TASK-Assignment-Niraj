# Basic Array Traversal in Python

# Define an array
arr = [10, 20, 30, 40, 50]

# Traverse the array and print each element
print("Array elements:")
for i in range(len(arr)):
    print(f"Element at index {i}: {arr[i]}")